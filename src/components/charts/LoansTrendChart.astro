<div id="loans-trend-chart" class="w-full h-full min-h-[300px]"></div>

<script>
  import ApexCharts from "apexcharts";

  const options = {
    series: [
      {
        name: "Préstamos",
        data: [31, 40, 28, 51, 42, 109, 100],
      },
      {
        name: "Devoluciones",
        data: [11, 32, 45, 32, 34, 52, 41],
      },
    ],
    chart: {
      height: 350,
      type: "area",
      fontFamily: "inherit",
      toolbar: {
        show: false,
      },
      animations: {
        enabled: true,
      },
    },
    dataLabels: {
      enabled: false,
    },
    stroke: {
      curve: "smooth",
      width: 2,
    },
    xaxis: {
      type: "datetime",
      categories: [
        "2018-09-19T00:00:00.000Z",
        "2018-09-19T01:30:00.000Z",
        "2018-09-19T02:30:00.000Z",
        "2018-09-19T03:30:00.000Z",
        "2018-09-19T04:30:00.000Z",
        "2018-09-19T05:30:00.000Z",
        "2018-09-19T06:30:00.000Z",
      ],
      labels: {
        style: {
          colors: "var(--m3-sys-on-surface-variant)",
        },
      },
      axisBorder: {
        show: false,
      },
      axisTicks: {
        show: false,
      },
    },
    yaxis: {
      labels: {
        style: {
          colors: "var(--m3-sys-on-surface-variant)",
        },
      },
    },
    grid: {
      borderColor: "var(--m3-sys-outline-variant)",
      strokeDashArray: 4,
      yaxis: {
        lines: {
          show: true,
        },
      },
    },
    tooltip: {
      theme: "dark", // Or custom CSS based on theme
      x: {
        format: "dd/MM/yy HH:mm",
      },
    },
    colors: ["#64B5F6", "#1976D2"],
    fill: {
      type: "gradient",
      gradient: {
        shadeIntensity: 1,
        opacityFrom: 0.7,
        opacityTo: 0.2,
        stops: [0, 90, 100],
      },
    },
    legend: {
      labels: {
        colors: "var(--m3-sys-on-surface)",
      },
    },
  };

  function renderChart() {
    const chartElement = document.querySelector("#loans-trend-chart");
    if (chartElement) {
      const chart = new ApexCharts(chartElement, options);
      chart.render();
    }
  }

  renderChart();
  document.addEventListener("astro:page-load", renderChart);
</script>
